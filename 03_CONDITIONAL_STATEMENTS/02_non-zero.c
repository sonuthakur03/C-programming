#include <stdio.h>
#include <conio.h>
int main()
{
    if (451)
    {
        printf("non-zero is true value in c\n");
    }
    if (2.45)
    {
        printf("non-zero is true value in c\n");
    }
    if ('c')
    {
        printf("non-zero is true value in c\n");
    }
    if (0)
    {
        printf("zero is false value in c"); // this code will not run
    }

    return 0;
}