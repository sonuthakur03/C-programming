// Write a program to check whether a number is divisible by 97 or not.

#include <stdio.h>
#include <conio.h>
int main()
{
    int a = 551799;
    printf("The remainder of a %% 97 is %d", a % 97);
    printf("\nIf remainder is 0 then number is divisible else its not");
    return 0;
}